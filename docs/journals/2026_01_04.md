- Added and cleaned up API model functions to create, update, and delete pipelines and processes.
- Refactored the specs check and the schema validation into the API section, leaving the augmentation of the processes in the media converter component.
	- The idea with this is: is it better to leave the processing and augmenting of the pipeline and the processes with the media converter components since they're going to be the jobs that are processing these things? Right now, it's a little confusing that the specs are living in the model section in the base.
	- I think it would be better to move everything into the API model as far as augmentation and any preprocessing.
	- The question then is, what is the best place for the specs? Is it better to link those to the media converter process and pipeline components, and then leave the preprocessing still within the API model section? So this part needs some more thinking.
- [[Next Steps]]
	- TODO Create an actual pipeline and proxy that correctly launches the proxy process and proxies a file.
	- TODO Create the cron job that pulls out open processes.
	- TODO Upon the creation of a new job in the Goose job scheduler, we should update the process status so that we don't pick up the job again. We should think through all of the states that the job can be in and then link that to the process.
	- TODO Check for existing cron job that is the core loop and don't create new one if it already exists.