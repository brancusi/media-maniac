- Make a queue for the processor heavy tasks and create a worker that has a thread pool of 1 so we don't kill the system
	- Should we queue based on the type of task? proxy vs stills?
- Create a cron job that puts jobs on the queue
- How do we deal with pulling jobs that are already in the queue?
	-